int somme,produit,quotient,reste,difference ;
char letter, digit, punct;
int uppercase;
    
int main(void)  {
   somme=12;
   produit=24;
   quotient=24/12;
   letter='c';
   digit='0';
   uppercase=letter+('A'-'a');
   reste=getint();
   punct=getchar();
   putint();
   putchar();
   return 0;
}
