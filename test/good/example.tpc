int globalVar;

int add(int a, int b) {
    return a + b;
}

void printResult(int result) {
    if (result > 0) {
        return;
    } else {
        return;
    }
}

int main(void) {
    int sum, x, y;
    x = 10;
    y = 20;
    sum = add(add(x,y), y);
    printResult(sum);
    return 0;
}
